
@model Buscador.ViewModels.SolicitacaoViewModel

@{
    ViewData["Title"] = "Minhas Solicitacoes";
}

<div class="d-none d-sm-block">
    <nav aria-label="breadcrumb">
        <ol class="breadcrumb">
            <li class="breadcrumb-item"><a asp-action="Index" asp-controller="Home" class="color-theme"><em class="mdi mdi-home"></em> Início</a></li>
            <li class="breadcrumb-item active color-theme" aria-current="page">Solicitarcao</li>
        </ol>
    </nav>
</div>
<div class="d-sm-none">
    <a asp-action="Index" asp-controller="Home" aria-label="Botão Início" class="breadcrumb-inicio color-theme"><em class="mdi mdi-chevron-left"></em> Início</a>
</div>
<hr />

<div class="mb-3">
    @*<span class="text-mute">Seja bem-vindo(a), </span><span><strong>@Model.FirstOrDefault().Cliente.Nome</strong></span>*@
</div>

<div class="d-flex justify-content-between">
    <h3>@ViewData["Title"]</h3>
</div>

@*@if (Model.Any())
{
    @foreach (var item in Model)
    {
        <div class="card mb-2">
            <div class="card-body">
                <div class="row">
                    <input asp-for="@item.NomeSolicitante" value="@item.Cliente.Nome" class="form-control" type="hidden" />
                    <input asp-for="@item.DocumentoSolicitante" value="@item.Cliente.Documento" class="form-control" type="hidden" />



                    <a class="btn btn-outline-default" asp-action="Detalhes" asp-controller="Servico" asp-route-Id="">Voltar</a>
                    <button type="submit" class="btn btn-default">Solicitar</button>


                </div>

            </div>
        </div>
    }
}
else
{
    <div class="row mt-3">
        <div class="col-md col-sm-12 mb-2">
            <div class="card">
                <div class="card-body">
                    <h4>Você não fez nenhuma Solicitação.</h4>
                </div>
            </div>
        </div>
    </div>
}*@
